language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-knockout-custom-binding' ]; then cd .. && eval "mv $currentfolder generator-knockout-custom-binding" && cd generator-knockout-custom-binding; fi
